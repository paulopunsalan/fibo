---

---

<html lang="en">
	<head>
		<meta charset="utf-8" />
		<meta name="viewport" content="width=device-width" />
		<meta name="generator" content={Astro.generator} />
		<title>a flower i call fibo</title>

		<style>
			body {
				position: absolute;
				background-color: black;

				margin: 0;

				width: 100vw;
				height: 100vh;

				font-family: 'Courier New', Courier, monospace;
				color: darkgray;
			}

			a {
				padding-top: 4px;
				margin: 8px;
				color: gray;
			}

			a:visited {
				color: gray;
			}

			span, input[type="color"] {
				margin-left: 8px;
			}

			canvas {
				position: absolute;

				top: 8px;
				left: 60px;

				transform: translate(-50%, -50%);
				background-color: transparent;
			}

			div.stem {
				background-image: url("/fibo/stem.png");
				background-size: contain;
				background-position: bottom;

				width: 198.5px;
				height: 430.5px;

				position: fixed;
				bottom: 0;
				left: calc(50% + 48px);
				transform: translateX(-50%);

				transform-origin: center bottom;

				animation: sway alternate 2s ease-in-out infinite;

				filter: brightness(200%);
			}

			canvas#fibo-2 {
				transform: translate(-50%, -50%) scaleX(-1.2) scaleY(1.2);
			}

			@keyframes sway {
				0% {
					transform: translateX(-50%) rotate(2deg);
				}
				100% {
					transform: translateX(-50%) rotate(-2deg);
				}
			}

			@media screen and (max-height: 850px) {
				div.stem {
					bottom: -100px;
				}
			}
		</style>
	</head>
	<body>
		<br>
		<a href="https://github.com/paulopunsalan/fibo">source code</a>

		<br><br>

		<input type="color" name="Color 1" id="col1" autocomplete="off" />
		<input type="color" name="Color 2" id="col2" autocomplete="off" />

		<br><br>

		<span>Modifier 1: </span>
		<input type="number" name="" id="mod1" autocomplete="off" step="0.0001" />
		<input type="checkbox" name="Mod1 Check" id="mod1-check" checked autocomplete="off" />

		<br>

		<span>Modifier 2: </span> 
		<input type="number" name="" id="mod2" autocomplete="off" step="0.0001">
		<input type="checkbox" name="Mod2 Check" id="mod2-check" checked autocomplete="off" />

		<br><br>

		<span>Size: </span> 
		<input type="number" name="" id="size" autocomplete="off" step="0.1" value="4">

		<br>

		<span>Space: </span> 
		<input type="number" name="" id="space" autocomplete="off" step="0.1" value="5">

		<div class="stem">
			<canvas width="500" height="500" id="fibo-2"></canvas>
			<canvas width="500" height="500" id="fibo"></canvas>
		</div>

		<script src="../js/fibo.js"></script>
	</body>
</html>
